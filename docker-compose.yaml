version: '3.8'

services:
  # frontend:
  #   build:
  #     context: ./frontend
  #   ports:
  #     - "3000:3000" # ou porta do seu front
  #   depends_on:
  #     - backend

  # backend:
  #   build:
  #     context: ./backend
  #   ports:
  #     - "8080:8080" # ou a porta da sua API
  #   depends_on:
  #     - db
  #   environment:
  #     DATABASE_URL: postgres://postgres:postgres@db:5432/app_db
  #     # Adicione outras variáveis se precisar

  db:
    image: postgres:16
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: highcapital
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
